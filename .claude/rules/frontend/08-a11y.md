# Accessibility (A11y)

Minimum requirements:

- Proper labels for inputs (`label` + `htmlFor`)
- Keyboard focus visible (`focus:ring`)
- `aria-*` where needed (dialogs, alerts)

### Modal confirm

Redeem flow requires a **2-step confirm** modal:
- explain irreversible action
- cancel must be easy to hit
