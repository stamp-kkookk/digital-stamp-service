# Routing & Navigation

Use React Router.

## Route grouping (suggested)

- `/c/*` : customer
- `/o/*` : owner/backoffice
- `/t/*` : store terminal

## Navigation rules

- Keep route params explicit (e.g., `storeId`, `stampCardId`).
- Avoid deep nesting unless it improves clarity.
